version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    depends_on:
      - mongodb
    restart: unless-stopped
  mongodb:
    image: mongo
    expose:
      - 27017
    restart: unless-stopped
