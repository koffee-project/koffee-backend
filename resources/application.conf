ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [
            eu.yeger.ApplicationKt.mainModule,
            eu.yeger.authentication.AuthenticationKt.authenticationModule,
            eu.yeger.routing.RoutingKt.routingModule
         ]
    }
}
